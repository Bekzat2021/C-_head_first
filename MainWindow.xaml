<Window x:Class="PracticeUsingIfElse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PracticeUsingIfElse"
        mc:Ignorable="d"
        Title="MainWindow" Height="320" Width="640">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="ChangeTheLable" Content="Change the label if checked" HorizontalAlignment="Center" VerticalAlignment="Center" Width="172"  AutomationProperties.Name="changeText" Click="Button_Click"/>
        <CheckBox x:Name="EnableLabelChange" Content="Enable label changing" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"  AutomationProperties.Name="enableCheckBox"/>
        <TextBlock x:Name="Label1" HorizontalAlignment="Center" Grid.Row="1" TextWrapping="Wrap" Text="Press the button to change my text"
                   VerticalAlignment="Center" AutomationProperties.Name="labelToChange" Grid.ColumnSpan="2" Width="258" FontSize="16"/>

    </Grid>
</Window>
